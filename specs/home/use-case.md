# UC-HOME1 — Land on the plugin's home tab

## System context

Landing SPA of the **worktable** plugin (tab "Home", dashboard id `home`, the plugin's default tab). Does not read or write any WorkTable table.

## Goal

Give the operator a landing page when opening the plugin. Currently just a welcome message — placeholder, content to be defined later.

## Primary Actor

CAMILA WorkTable administrator / developer.

## Preconditions

- User is logged into CAMILA WorkTable with access to the worktable plugin.

## Postconditions — Success

- A welcome message is shown.

## Main Success Scenario

### Step 1 — Land on the Home tab

1. User opens the "Home" tab (default tab of the plugin), or the plugin with no `dashboard` param.
2. The page shows a static welcome title and message.

## Extensions

None yet — no interaction, no network calls, nothing that can fail.

## Status

Placeholder only. Revisit once the intended content for this page is decided.
